_schema-version: 3.3.0
ID: EnterPrice_Product
description: A simple CAP project.
version: 1.0.0-SNAPSHOT
modules:
- name: EnterPrice_Product-srv
  type: java
  path: srv
  properties:
    JBP_CONFIG_COMPONENTS: 'jres: [''com.sap.xs.java.buildpack.jre.SAPMachineJRE'']'
    JBP_CONFIG_SAP_MACHINE_JRE: '{ version: 21.+ }'
    SPRING_PROFILES_ACTIVE: cloud,sandbox
  requires:
  - name: EnterPrice_Product-auth
  # - name: EnterPrice_Product-db
  provides:
  - name: srv-api
    properties:
      srv-url: ${default-url}
  parameters:
    buildpack: sap_java_buildpack_jakarta
    instances: 1
  build-parameters:
    build-result: target/*-exec.jar
    builder: custom
    commands:
    - mvn clean package -DskipTests=true --batch-mode
# - name: EnterPrice_Product-db-deployer
#   type: hdb
#   path: db
#   requires:
#   - name: EnterPrice_Product-db
#   parameters:
#     buildpack: nodejs_buildpack
#   build-parameters:
#     builder: custom
#     commands:
#     - npm run build
- name: EnterPrice_Product-destination-content
  type: com.sap.application.content
  requires:
  - name: EnterPrice_Product-destination-service
    parameters:
      content-target: true
  - name: EnterPrice_Product_html_repo_host
    parameters:
      service-key:
        name: EnterPrice_Product_html_repo_host-key
  - name: EnterPrice_Product-auth
    parameters:
      service-key:
        name: EnterPrice_Product-auth-key
  parameters:
    content:
      instance:
        destinations:
        - Name: BNApprouter_EnterPrice_Product_html_repo_host
          ServiceInstanceName: EnterPrice_Product-html5-app-host-service
          ServiceKeyName: EnterPrice_Product_html_repo_host-key
          sap.cloud.service: BNApprouter
        - Authentication: OAuth2UserTokenExchange
          Name: BNApprouter_EnterPrice_Product_auth
          ServiceInstanceName: EnterPrice_Product-auth
          ServiceKeyName: EnterPrice_Product-auth-key
          sap.cloud.service: BNApprouter
        existing_destinations_policy: ignore
  build-parameters:
    no-source: true
- name: EnterPrice_Product-app-content
  type: com.sap.application.content
  path: .
  requires:
  - name: EnterPrice_Product_html_repo_host
    parameters:
      content-target: true
  build-parameters:
    build-result: resources
    requires:
    - artifacts:
      - purchaseorder.zip
      name: purchaseorder
      target-path: resources/
- name: purchaseorder
  type: html5
  path: app/purchase_order
  build-parameters:
    build-result: dist
    builder: custom
    commands:
    - npm install
    - npm run build:cf
    supported-platforms: []
resources:
- name: EnterPrice_Product-auth
  type: org.cloudfoundry.managed-service
  parameters:
    config:
      oauth2-configuration:
        credential-types:
        - binding-secret
        - x509
      tenant-mode: dedicated
      xsappname: EnterPrice_Product-${org}-${space}
    path: ./xs-security.json
    service: xsuaa
    service-plan: application
# - name: EnterPrice_Product-db
#   type: com.sap.xs.hdi-container
#   parameters:
#     service: hana
#     service-plan: hdi-shared
- name: EnterPrice_Product-destination-service
  type: org.cloudfoundry.managed-service
  parameters:
    config:
      HTML5Runtime_enabled: true
      init_data:
        subaccount:         # <--- UPDATED TO SUBACCOUNT
          existing_destinations_policy: update
          destinations:
          - Authentication: NoAuthentication
            Name: ui5
            ProxyType: Internet
            Type: HTTP
            URL: https://ui5.sap.com
          - Authentication: NoAuthentication
            HTML5.DynamicDestination: true
            HTML5.ForwardAuthToken: true
            html5.forwardAuthToken: true # Added lowercase variant for absolute compatibility
            Name: srv-api
            ProxyType: Internet
            Type: HTTP
            URL: ~{srv-api/srv-url}
      version: 1.0.0
    service: destination
    service-name: EnterPrice_Product-destination-service
    service-plan: lite
  requires:
  - name: srv-api
- name: EnterPrice_Product_html_repo_host
  type: org.cloudfoundry.managed-service
  parameters:
    service: html5-apps-repo
    service-name: EnterPrice_Product-html5-app-host-service
    service-plan: app-host
parameters:
  deploy_mode: html5-repo
  enable-parallel-deployments: true
